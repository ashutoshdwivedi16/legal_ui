import type { MenuItem } from '@core/navigation/types';

const menu: MenuItem = {
  id: 'communication/reports',
  label: 'Reports',
  path: '/communication/reports',
  order: 4,
};

export default menu;
