import type { MenuItem } from '@core/navigation/types';

const menu: MenuItem = {
  id: 'communication/transactions',
  label: 'Transactions',
  path: '/communication/transactions',
  order: 3,
};

export default menu;
