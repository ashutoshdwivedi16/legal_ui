import { MarkerType } from "@xyflow/react";

const edgeType = "smoothstep";
const markerEnd = {
  type: MarkerType.ArrowClosed,
  width: 25,
  height: 25,
};
export const edgeParams = {
  type: edgeType, markerEnd,
};