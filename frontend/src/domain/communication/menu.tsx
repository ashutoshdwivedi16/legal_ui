import { Mail } from 'lucide-react';
import type { MenuItem } from '@core/navigation/types';

const menu: MenuItem = {
  id: 'communication',
  label: 'Communication',
  icon: Mail,
  order: 10,
};

export default menu;
