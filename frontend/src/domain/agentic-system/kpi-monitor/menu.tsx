import type { MenuItem } from '@core/navigation/types'

const menu: MenuItem = {
  id: 'agentic-system/kpi-monitor',
  label: 'KPI Monitor',
  path: '/agentic-system/kpi-monitor',
  order: 1,
}

export default menu
