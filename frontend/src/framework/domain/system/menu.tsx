import { Settings } from 'lucide-react'
import type { MenuItem } from '../../core/navigation/types'

const menu: MenuItem = {
  id: 'system',
  label: 'System',
  icon: Settings,
  order: 200,
}

export default menu
