import { Box } from 'lucide-react';
import type { MenuItem } from '@core/navigation/types';

const menu: MenuItem = {
  id: 'sample',
  label: 'Sample',
  icon: Box,
  order: 20,
};

export default menu;
