apiVersion: apps/v1
kind: Deployment
metadata:
  name: lg-admin-admin-service-fe-dev
  namespace: admin-service
  labels:
    app.kubernetes.io/instance: lg-admin-admin-service-fe-dev
    app.kubernetes.io/name: lg-admin-admin-service-fe-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: lg-admin-admin-service-fe-dev
      app.kubernetes.io/name: lg-admin-admin-service-fe-dev
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: lg-admin-admin-service-fe-dev
        app.kubernetes.io/name: lg-admin-admin-service-fe-dev
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/name
                    operator: In
                    values:
                      - lg-admin-admin-service-fe-dev
              topologyKey: "kubernetes.io/hostname"
      containers:
        - name: lg-admin-admin-service-fe-dev
          image: IMAGE_VERSION
          ports:
          - containerPort: 80
#      nodeSelector:
#        subnets_good: private1_private2
